{
  "name": "frontend-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dev-eco-feria && npm install --legacy-peer-deps && NODE_OPTIONS=--max-old-space-size=2048 npm run build",
    "start": "cd dev-eco-feria && npm run serve:ssr:dev-eco-feria"
  }
}
